import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.boldplatypusdrift',
  appName: 'bold-platypus-drift',
  webDir: 'dist'
};

export default config;
